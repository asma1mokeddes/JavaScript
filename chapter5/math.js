// object math
const a = Math.pow(2,3);
const pi = Math.pi;
const entiersup = Math.pceil(1.0); // renvoie le nombre entier supérieur -> 2
const entierinf = Math.floor(2.0); // renvoie le nombre entier inférieur -> 1
const max = Math.max(2,1,4,3); // renvoie le maximum  -> 4
const min = Math.min(8,6,2); // renvoie le minimum -> 2
const rand = Math.random(); // nombre décimal entre 0 et 1 
const randMax = Math.round(Math.random() * 100); // nombre entre 0 et 100 
